#!/usr/bin/env bash
# bin/detect <build-dir>


source $1/.build_envs


if [ -f $1/back-end/package.json ]; then
  echo "$1/$BUILD_DIR" && exit 0
elif [ -f $1/$BUILD_DIR/server.js ]; then
  echo "Node.js" && exit 0
else
  echo "no" && exit 1
fi
